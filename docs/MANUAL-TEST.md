# 手動測試檢查表

版本：0.1.0 (main 2026-09-17)。互動版（可直接勾結果）在 Claude 的 artifact 頁面；這份是同一份案例的純文字版。

## 環境準備

- Windows 11，16 GB 以上記憶體。
- 執行檔：`D:\AndroidStudioProjects\claude_code\myterminal\myterminal-0.1.0-portable.exe`（免安裝）或 `dist\myterminal-0.1.0-setup.exe`（安裝版）。
- WSL 已裝 Ubuntu；PuTTY 的 `plink.exe` 在 `C:\Program Files\PuTTY\` 或 PATH 上（SSH 案例用）。SSH 案例可用本機 sshd：`wsl.exe -u root -e bash scripts/wsl-sshd-setup.sh` 會開 127.0.0.1:2222，帳號 `mtssh`，密碼 `mtssh-e2e`。
- `claude`（已登入 Max）、`codex`（已登入 ChatGPT）、`muse`（已裝，未登入）、`opencode`（已裝，免費模型不需金鑰）都在 PATH 上。裝新 CLI 後要重開 myterminal。
- 會用到 Claude / Codex 的案例只消耗訂閱額度，不會扣款；OpenCode 用 `opencode/mimo-v2.5-free` 免費模型，只需要網路。
- 工作目錄請用真實長路徑（例如 `D:\tmp\mt-test`），不要用 `C:\Users\ROBERT~1\…` 這種短檔名。
- 資料檔位置：`%APPDATA%\myterminal\`（`profiles.json`、`workflows.json`、`workflow-runs.json`、`cli-auth.json`）；紀錄檔在 `%USERPROFILE%\myterminal-logs\`。要從乾淨狀態開始，先關 app 再把 `%APPDATA%\myterminal` 改名備份。

標了「需要 …」的案例會用到帳號、額度、網路或會動到系統；做不到就記「略過」與原因。


## A. 安裝與啟動

### A1 免安裝版啟動

步驟：
1. 雙擊 `myterminal-0.1.0-portable.exe`。
2. 等 30 到 60 秒。

預期：
- 出現主視窗：上方工具列、中間空白終端機區與「還沒有工作階段」提示、右側工作階段 / 已儲存連線 / 工作流三個區塊、最底下四個 CLI 狀態標籤。
- 不出現任何 JavaScript 錯誤對話框。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### A2 安裝版安裝與啟動（需要 會安裝到目前使用者）

步驟：
1. 執行 `dist\myterminal-0.1.0-setup.exe`。
2. 從桌面或開始功能表捷徑啟動。
3. 「應用程式與功能」裡移除。

預期：
- 不需要系統管理員權限就能裝完；啟動幾乎不用等。
- 移除後捷徑消失，`%APPDATA%\myterminal` 的資料檔仍在（設定不會被刪）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### A3 SmartScreen 提示

步驟：
1. 第一次執行任一 exe。

預期：
- 若出現「Windows 已保護您的電腦」，按「其他資訊」→「仍要執行」可以繼續；沒有其他警告。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### A4 視窗最小尺寸

步驟：
1. 用滑鼠把視窗往左上角拖到最小。

預期：
- 視窗停在約 900×560，不會更小；工具列最右邊的「清除畫面」與主題選單仍看得到、按得到。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### A5 CLI 狀態標籤

步驟：
1. 啟動後看側欄最底下。

預期：
- 顯示 `Claude · Max 訂閱`、`Codex · ChatGPT 訂閱`、`Muse · 未登入`（或你的實際狀態）、`OpenCode · 未登入`。
- 如果某支 CLI 沒裝，顯示「找不到指令」而不是空白或錯誤。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## B. 主題

### B1 三種主題切換

步驟：
1. 工具列右側的主題下拉選單依序選 淺色、暖色、深色。

預期：
- 整個視窗（工具列、側欄、終端機底色、對話框）立刻換色，三種明顯不同；暖色是暖炭底加琥珀色。
- 沒有任何元素維持舊主題的顏色。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### B2 主題記住

步驟：
1. 選暖色。
2. 關掉 app 再開。

預期：
- 重開後直接是暖色。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### B3 主題套到畫布與對話框

步驟：
1. 選淺色。
2. 按右側「工作流」旁的「編輯」打開畫布；再按「新連接」開對話框。

預期：
- 畫布背景、卡片、連線與對話框都是淺色系，文字可讀。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## C. 工作階段

### C1 PowerShell

步驟：
1. 新連接 → 類型 PowerShell → 建立。
2. 輸入 `echo HELLO` Enter。

預期：
- 幾秒內出現 `PS …>` 提示字元；`HELLO` 印出；側欄多一列「PowerShell 1」，綠點「執行中」，標籤 PowerShell。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C2 兩個工作階段切換

步驟：
1. 再開一個 PowerShell，各自輸入不同的 `echo` 內容。
2. 在側欄點兩列來回切換。

預期：
- 第二個叫「PowerShell 2」；切換時各自的內容還在，不會混在一起或被清掉。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C3 WSL 預設發行版

步驟：
1. 新連接 → WSL，發行版留空 → 建立。
2. 輸入 `uname -a`。

預期：
- 40 秒內出現 `$` 提示字元；輸出含 `Linux`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C4 WSL 指定發行版

步驟：
1. 新連接 → WSL，發行版填 `Ubuntu`，工作目錄填 `/tmp` → 建立。
2. 輸入 `pwd`。

預期：
- 進到 `/tmp`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C5 WSL 不存在的發行版

步驟：
1. 新連接 → WSL，發行版填 `NoSuchDistro` → 建立。

預期：
- 工作階段建立後終端機印出 WSL 的錯誤（含 `WSL_E_DISTRO_NOT_FOUND` 之類），那一列變「已結束」；app 不會跳錯誤框、還能繼續操作。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C6 SSH 首次連線（需要 sshd）

步驟：
1. 新連接 → SSH，主機 `localhost`，連接埠 `2222`，使用者 `mtssh` → 建立。
2. 出現 host key 詢問時打 `y` Enter。
3. 出現密碼提示時輸入 `mtssh-e2e`。
4. 輸入 `echo SSH_OK; hostname`。

預期：
- host key 只有第一次問；密碼提示是 plink 在終端機裡問的；`SSH_OK` 印出。
- 第一行指令沒有被吃掉（不需要先按一次 Enter）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C7 SSH 離線（需要 sshd）

步驟：
1. 在 C6 的工作階段輸入 `exit`。

預期：
- 那一列變「已結束 (0)」，畫面內容保留可回看。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C8 SSH 對話框驗證

步驟：
1. 新連接 → SSH，主機留空 → 建立。
2. 主機填 `x`，連接埠改成 `70000` → 建立。
3. 連接埠打 `abc` → 建立。
4. 每一步之後按「取消」。

預期：
- 三種都留在對話框並顯示對應錯誤（請輸入主機位址 / 連接埠必須介於 1 到 65535 / 請輸入連接埠）。
- 「取消」在任何狀態下都關得掉對話框，不會卡住。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C9 自訂命令與離開碼

步驟：
1. 新連接 → 自訂命令，執行檔 `cmd.exe`，參數 `/c echo CUSTOM_OK && exit 7` → 建立。

預期：
- 終端機印 `CUSTOM_OK`；那一列顯示「已結束 (7)」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C10 自訂命令的引號參數

步驟：
1. 自訂命令，執行檔 `cmd.exe`，參數 `/c echo "a b" c`。

預期：
- 印出 `"a b" c` 或 `a b c`（cmd 語意），不會出現反斜線 `\"`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C11 不存在的執行檔

步驟：
1. 自訂命令，執行檔 `no-such-exe-123`。

預期：
- 顯示含檔名的錯誤（例如「找不到執行檔：no-such-exe-123」），不是空白的 `File not found:`，也不是一長串 `Error invoking remote method`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C12 工作目錄不存在

步驟：
1. 新連接 → PowerShell，工作目錄填 `D:\no-such-dir-98765` → 建立。

預期：
- 顯示「工作目錄不存在：D:\no-such-dir-98765」，不建立工作階段。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C13 Claude 互動工作階段

步驟：
1. 新連接 → Claude（基礎 shell PowerShell）→ 建立。
2. 看到 Claude Code 畫面後，輸入 `/exit` 或按 Ctrl+C 兩次離開。

預期：
- 先開 PowerShell 再自動送出 `claude`，出現 Claude Code 的歡迎畫面；離開後那一列變已結束或回到提示字元。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C14 Codex 互動工作階段

步驟：
1. 新連接 → Codex → 建立。

預期：
- 出現 Codex 的 TUI（可能先出現「Update available，Press enter to continue」）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C15 Muse 互動工作階段

步驟：
1. 新連接 → Muse（基礎 shell PowerShell）→ 建立。

預期：
- 出現 `Muse Code 1.3.0` 的畫面；未登入時會問「Log in with browser / Set an API key」。
- 基礎 shell 選 WSL 也開得起來。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C16 OpenCode 互動工作階段

步驟：
1. 新連接 → OpenCode → 建立。

預期：
- 出現 OpenCode 的 TUI（底部有 Ask anything / tab agents）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C17 關閉工作階段

步驟：
1. 在側欄某列按 ✕。

預期：
- 那列消失、數量減一；如果它是目前顯示的，畫面切到另一個工作階段；全關後回到「還沒有工作階段」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C18 已結束的畫面保留

步驟：
1. 在 PowerShell 輸入 `exit`。

預期：
- 那一列變「已結束 (0)」灰點；點它仍看得到之前的輸出；按 ✕ 才移除。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### C19 關視窗時終止全部

步驟：
1. 開兩個 PowerShell，其中一個跑 `ping -t 127.0.0.1`。
2. 關閉 myterminal 視窗。

預期：
- app 在幾秒內完全結束；工作管理員裡沒有殘留的 myterminal 或 powershell 行程。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## D. 工具列

### D1 輸入字：單行與 Ctrl+Enter

步驟：
1. 開 PowerShell，按「輸入字」。
2. 輸入 `echo ONE`，按「送出」。
3. 輸入 `echo TWO`，改按 Ctrl+Enter。

預期：
- 面板上方顯示「送到 PowerShell 1」；`ONE` 執行並印出；輸入框清空。
- 輸入區下面那一行小字寫著「Ctrl+Enter 送出」。
- Ctrl+Enter 跟按「送出」一樣：`TWO` 執行並印出，輸入框清空，游標還在輸入框裡。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D2 輸入字：多行給 shell

步驟：
1. 輸入框打 `echo LINE_ONE`，按單獨的 Enter，再打 `echo LINE_TWO`，最後按 Ctrl+Enter。

預期：
- 單獨的 Enter 只是在輸入框裡換行，沒有送出。
- Ctrl+Enter 之後兩行依序都執行，各自印出；提示字元不會卡在 `>>` 續行狀態。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D3 輸入字：多行給 Claude（需要 Claude 額度）

步驟：
1. 開 Claude 工作階段，等歡迎畫面。
2. 輸入框打兩行：`只回覆 OK。` 換行 `不要做別的事。`，送出。

預期：
- 兩行進到 Claude 的同一個輸入框成為一段多行提示，然後送出一次；Claude 回覆 OK。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D4 複製文字

步驟：
1. 在 PowerShell 印一行 `echo COPY_ME_42`。
2. 用滑鼠在終端機拖曳選取 `COPY_ME_42`，按「複製文字」。
3. 到記事本貼上。

預期：
- 貼出來的是 `COPY_ME_42`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D5 貼上：單行

步驟：
1. 在記事本複製 `echo PASTE_OK`。
2. 回 myterminal 按「貼上」，再按 Enter。

預期：
- `PASTE_OK` 印出。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D6 貼上：多行

步驟：
1. 複製三行：`echo A` / `echo B` / `echo C`。
2. 按「貼上」，再按 Enter。

預期：
- 三行依序執行，順序 A B C；不會顛倒、不會只執行一行。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D7 貼上：大段文字

步驟：
1. 複製一段約 50 KB 的單行文字（例如在記事本產生）。
2. 按「貼上」。

預期：
- 幾秒內貼完，app 期間可操作；按 Esc 或 Ctrl+C 可清掉再繼續用。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D8 紀錄

步驟：
1. 在 PowerShell 按「紀錄」。
2. 輸入 `echo LOG_MARK_123`。
3. 再按一次「紀錄」停止，再輸入 `echo AFTER_STOP`。
4. 打開 `%USERPROFILE%\myterminal-logs\` 最新的 `.log`。

預期：
- 按下後按鈕變紅色「紀錄中」，側欄那列出現紅點。
- 檔名是 `PowerShell 1-<本地日期時間>.log`（時間是本地時間）；內容含 `LOG_MARK_123`，不含 `AFTER_STOP`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D9 已結束不能開紀錄

步驟：
1. 在已結束的工作階段上看「紀錄」按鈕。

預期：
- 按鈕是停用狀態，按了沒反應，不會產生 0 byte 的紀錄檔。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D10 清除畫面

步驟：
1. 有輸出時按「清除畫面」。

預期：
- 畫面清空，只剩提示字元；底層 shell 沒被重啟（之前設的變數還在）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D11 對話框 Enter 等於建立

步驟：
1. 新連接 → 在「名稱」欄輸入 `mine`，按 Enter。

預期：
- 建立工作階段（名稱 mine），不是取消。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D12 對話框 Esc

步驟：
1. 新連接 → 按 Esc。

預期：
- 對話框關閉，什麼都不建立。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D13 輸入字面板不會被搶焦點

步驟：
1. 開 PowerShell，按「輸入字」，在面板裡打 `HELLO`（不要送出）。
2. 按一下「紀錄」再按一次關掉（製造一次狀態變化）。
3. 繼續在面板打字。

預期：
- 打的字都留在面板裡，終端機的命令列沒有出現 HELLO；狀態變化後游標仍在面板。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D14 終端機中文輸入

步驟：
1. 在 PowerShell 用輸入法打 `echo 你好` 按 Enter。
2. 開 Claude 工作階段，在它的輸入框用輸入法打「你好」（不要送出，Ctrl+C 清掉）。

預期：
- PowerShell 印出 `你好`，沒有重複、沒有少字、候選字視窗貼在游標旁。
- Claude 的輸入框顯示「你好」一次。若有異常，請在備註寫：字重複 / 少字 / 候選字視窗跑掉 / Ctrl+C 沒反應，以及在哪種畫面。
- 候選字視窗的位置請特別看：畫面在刷新（例如跑 `dir` 或 Claude 正在輸出）的同時開始組字，以前可能會跑到舊座標（上游 xterm.js #5734）。現在 app 會在組字一開始就把隱藏輸入區搬回游標那一格；還是跑掉的話請記下當時畫面在做什麼。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### D15 輸入字：歷史、草稿、送出後保留、拉高度

步驟：
1. 開 PowerShell 1，按「輸入字」，送出 `echo H1`，再送出 `echo H2`。
2. 在空的輸入框按 ↑ 兩次、再按 ↓ 兩次。
3. 叫回 `echo H1` 之後把它改成 `echo H1X`（不要送出），按 ↓ 到空白那一格，再按 ↑ 回來看。
4. 打一段多行（三行），把游標放在中間那行，按 ↑ 和 ↓。
5. 輸入框打 `DRAFT_1`（不送出），新開 PowerShell 2，打 `DRAFT_2`（不送出），切回 1 再切回 2。
6. 勾「送出後保留」，送出 `echo KEEP`，再按一次「送出」。
7. 拉輸入區右下角把面板拉高一些，在終端機跑 `dir`；關掉 app 再開。

預期：
- 步驟 2：↑ 依序叫回 `echo H2`、`echo H1`，再按 ↑ 就不動了；↓ 又走回 `echo H2`、然後是空白。
- 步驟 3：改過的是副本，按 ↑ 回來看到的是 `echo H1X`；之後再送出任何一句，歷史裡那一筆回到原文 `echo H1`。
- 步驟 4：游標在中間行時 ↑ ↓ 只是在行之間移動，不會把整段字換成歷史；游標在第一行再按 ↑ 才叫歷史（最後一行按 ↓ 同理）。
- 步驟 5：切回 PowerShell 1 看到 `DRAFT_1`，切回 2 看到 `DRAFT_2`，兩邊不互相干擾。
- 步驟 6：送出之後字還在框裡，再按一次就又跑了一次；取消勾選之後恢復成送完清空。
- 步驟 7：面板變高之後終端機跟著變矮，`dir` 的輸出換行正常；重開之後高度與「送出後保留」的勾選都跟關掉前一樣。
- 歷史與草稿只存在記憶體裡：重開 app 之後 ↑ 叫不回上一次的內容，這是正常的。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## E. 已儲存連線

### E1 儲存連線

步驟：
1. 新連接 → PowerShell，名稱 `我的 PS`，勾「儲存此連線設定」→ 建立。

預期：
- 工作階段建立，且「已儲存連線」多一列「我的 PS」，標籤 PowerShell。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### E2 點一下連線

步驟：
1. 點「已儲存連線」的「我的 PS」。

預期：
- 用同樣設定開一個新的工作階段。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### E3 刪除有確認

步驟：
1. 滑到那列按 ✕ → 先按「取消」再試一次按「確定」。

預期：
- 取消時保留；確定後消失，`profiles.json` 裡也沒有了。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### E4 重啟後保留

步驟：
1. 存兩個連線，關 app 再開。

預期：
- 兩個都還在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### E5 同名覆蓋

步驟：
1. 存一個名稱 `dup` 的 WSL 連線，再存一個名稱 `dup` 的 PowerShell 連線。

預期：
- 只剩一個 `dup`，是 PowerShell（已知行為：直接覆蓋、沒有確認，README 有寫）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### E6 名稱含中文與 emoji

步驟：
1. 名稱 `測試 🚀 名稱` 儲存。

預期：
- 側欄、已儲存連線、`profiles.json` 都正確顯示 / 儲存。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## F. CLI 設定

### F1 打開對話框

步驟：
1. 按側欄最底下的 ⚙。

預期：
- 四列 Claude / Codex / Muse / OpenCode，每列右上角是目前狀態；前三列有「登入 / API 金鑰」兩個選項，OpenCode 只有「API 金鑰」與供應商、型號欄位。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F2 儲存 API 金鑰

步驟：
1. Claude 列選「API 金鑰」，金鑰欄貼 `sk-ant-test-123`，按「儲存」。
2. 關掉對話框再打開。
3. 關 app 再開，再打開對話框。

預期：
- 儲存後 Claude 的狀態標籤變「API 金鑰」；金鑰欄顯示「已儲存」佔位，看不到原文。
- 重開 app 後仍是 API 金鑰、仍顯示已儲存。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F3 清除金鑰

步驟：
1. Claude 列按「清除金鑰」。

預期：
- 狀態回到「Max 訂閱」（登入），金鑰欄變回空白可輸入。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F4 金鑰真的注入（需要 一組假金鑰）

步驟：
1. Claude 選 API 金鑰、存假金鑰 `sk-ant-test-123`。
2. 新連接 → Claude → 建立，在 Claude 裡輸入任何問題。
3. 另外開一個 PowerShell，輸入 `echo $env:ANTHROPIC_API_KEY`。

預期：
- Claude 工作階段出現認證錯誤（假金鑰被用了），證明環境變數注入到 Claude 的工作階段。
- PowerShell 工作階段印出空白：金鑰不會注入其他類型的工作階段。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F5 Claude 登入流程（需要 會動到你的 Claude 登入）

步驟：
1. Claude 列選「登入」按「登入」。

預期：
- 開一個工作階段跑 `claude auth login`，瀏覽器打開；完成後工作階段結束，狀態標籤重新偵測。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F6 Codex 登入流程（需要 會動到你的 Codex 登入）

步驟：
1. Codex 列按「登入」。

預期：
- 工作階段跑 `codex login`，瀏覽器打開。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F7 Codex 金鑰的提示

步驟：
1. 看 Codex 列。

預期：
- 有一行提示：存過 ChatGPT 登入時它優先，要用金鑰請先 `codex logout`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F8 Muse 登入流程（需要 Meta 帳號）

步驟：
1. Muse 列按「登入」，在瀏覽器完成驗證碼。

預期：
- 工作階段跑 `muse login`；完成後狀態變成已登入（偵測 `%USERPROFILE%\.config\muse\auth.json`）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F9 Muse 金鑰（需要 META_API_KEY）

步驟：
1. Muse 選 API 金鑰、貼金鑰、儲存。
2. 新連接 → Muse。

預期：
- Muse 直接進入可用狀態，不再問登入。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F10 OpenCode 免費模型

步驟：
1. OpenCode 列供應商選「OpenCode 免費模型（不需金鑰）」，儲存。

預期：
- 金鑰欄隱藏；型號預填 `mimo-v2.5-free`；狀態標籤變 API 金鑰模式。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F11 OpenCode 供應商金鑰（需要 Anthropic 或 OpenAI 金鑰）

步驟：
1. 供應商選 Anthropic，貼金鑰，型號填 `claude-sonnet-4-5`，儲存。
2. 新連接 → OpenCode。

預期：
- 啟動指令變成 `opencode -m anthropic/claude-sonnet-4-5`；TUI 用該模型。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F12 裝新 CLI 後的偵測

步驟：
1. 把 `%LOCALAPPDATA%\Programs\muse` 暫時改名，重開 app 看 Muse 標籤；改回來，再重開。

預期：
- 改名時顯示「找不到指令」；改回並重開後恢復偵測。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### F13 在外面的終端機登入後按重新偵測

步驟：
1. app 開著，在別的終端機把 `%USERPROFILE%\.config\museuth.json` 改名（等於登出 Muse）。
2. 按頁尾那一行右邊的「重新偵測」。
3. 把檔案名字改回來，打開「CLI 設定」，按標題右邊的「重新偵測」。

預期：
- 按下去的當下四個標籤都寫「偵測中…」，兩顆「重新偵測」都暫時按不下去（連按不會多探一輪）。
- 第 2 步之後 Muse 標籤變成「未登入」，第 3 步之後變回原本的登入狀態 ——
  全程不必關掉 app 再開。
- 按頁尾的「重新偵測」不會順便打開「CLI 設定」對話框。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## G. Agent 任務

### G1 Claude 唯讀回答（需要 Claude 額度）

步驟：
1. 新連接 → Agent 任務，執行者 Claude，任務 `只回覆 AGENT_OK，不要做別的事`，工作目錄 `D:	mp\mt-test`（先建好），「權限」留「唯讀」→ 建立。

預期：
- 終端機第一行 `[claude] 任務：…`，接著 `AGENT_OK`，最後綠色 `✔ 完成 · 秒數 · ≈$金額 · session …`，然後「工作階段已結束」。
- 側欄那列標籤「Claude 任務」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G2 權限：可修改檔案（需要 Claude 額度）

步驟：
1. 同上但「權限」選「可修改檔案」，任務 `在工作目錄建立 hello.txt 內容 hi`。
2. 再跑一次選回「唯讀」，任務 `在工作目錄建立 blocked.txt`。

預期：
- 「權限」下拉有三個選項：唯讀 / 可修改檔案 / 完全放行（會執行任何指令）。
- 第一次 `hello.txt` 出現；第二次 `blocked.txt` 不出現，Claude 說明它在唯讀 / plan 模式。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G3 角色（需要 Claude 額度）

步驟：
1. Agent 任務，角色選「審查者」。
2. 觀察「允許修改檔案」勾選狀態。
3. 任務 `用一句話說明你現在扮演的角色`。

預期：
- 選審查者時「權限」自動變唯讀；選「工程師」自動變可修改檔案。
- 回答以審查者身分描述；側欄那列有「審查者」標籤。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G4 接手 Claude（需要 Claude 額度）

步驟：
1. G1 跑完後，滑到那列按「接手」。
2. 在新開的互動式 Claude 裡問 `你剛才回覆了什麼？`。

預期：
- 開了 `claude --resume <id>` 的互動式工作階段，它記得剛才的對話（回答 AGENT_OK）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G5 Codex 任務（需要 Codex 額度）

步驟：
1. Agent 任務，執行者 Codex，任務 `Reply with exactly CODEX_OK`。

預期：
- 約 1 到 2 分鐘內 `CODEX_OK` 與 `✔ 完成`；金額顯示 `≈$0.000`（Codex 只回 token 數，已知）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G6 接手 Codex（需要 Codex 額度）

步驟：
1. G5 之後按「接手」。

預期：
- 開 `codex resume <id>` 的互動式工作階段。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G7 OpenCode 任務（免費模型）（需要 網路）

步驟：
1. 先做 F10。
2. Agent 任務，執行者 OpenCode，任務 `Reply with exactly OPENCODE_OK`。

預期：
- `OPENCODE_OK` 與 `✔ 完成`，不需要任何金鑰。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G8 接手 OpenCode（需要 網路）

步驟：
1. G7 之後按「接手」。

預期：
- 開 `opencode --session <id>` 的 TUI，看得到剛才那段對話。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G9 Muse 任務（需要 Meta 帳號或金鑰）

步驟：
1. 先做 F8 或 F9。
2. Agent 任務，執行者 Muse，任務 `只回覆 MUSE_OK`。

預期：
- `MUSE_OK` 與 `✔ 完成`。若失敗，請把終端機最後幾行複製到備註（這條在開發機上沒跑過）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G10 空任務驗證

步驟：
1. Agent 任務，任務留空 → 建立。

預期：
- 對話框不關，顯示「請輸入任務內容」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G11 工作目錄不存在

步驟：
1. Agent 任務，工作目錄填 `D:\no-such-dir-98765`。

預期：
- 終端機顯示 `✘ 失敗：工作目錄不存在：…`，不是 `spawn claude ENOENT`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G12 金額標示

步驟：
1. 看 G1 結果列的金額，滑鼠停在工作流清單的金額上。

預期：
- 訂閱帳號的金額前有 `≈`；工作流清單的金額 tooltip 說明是估算、訂閱不另收費。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### G13 權限：完全放行才跑得了指令（需要 Claude 額度 ×2）

步驟：
1. 新連接 → Agent 任務，執行者 Claude，工作目錄 `D:	mp\mt-test`，「權限」選「完全放行（會執行任何指令）」，任務 `用 Bash 執行 git --version 並回覆輸出` → 建立。
2. 同樣的任務再跑一次，「權限」改成「可修改檔案」。
3. 第一次那一列滑過去按「接手」，看新工作階段第一行送進去的啟動指令。

預期：
- 第一次：任務標頭是 `[claude] 任務：… [完全放行]`，輸出裡看得到 `git version 2.x`。
- 第二次：沒有版本號，Claude 說這個 Bash 指令被權限擋下來 / 需要核准。
- 第一次那一列的類型標籤旁邊有一個紅色的「完全放行」標記；第二次沒有。
- 接手的啟動指令是 `claude --resume <id> --dangerously-skip-permissions`，進去之後要 Claude 跑 `git --version` 還是跑得動；對照組：去接 G1（唯讀）那一次，啟動指令只有 `claude --resume <id>`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## H. 工作流

### H1 執行範本到等待批准（需要 Claude 額度 ×2）

步驟：
1. 側欄「工作流」按「執行工作流」，選「實作 → 審查 → 批准」，任務 `在工作目錄建立 wf.txt 內容 done`，工作目錄 `D:\tmp\mt-test`，用量上限留空 → 開始。

預期：
- 清單出現這次執行：狀態「執行中」，節點列依序 實作 → 審查 → 檢查 亮起；每個 agent 節點都是一個工作階段（名稱「範本名 · 節點名」）。
- 約 1 到 2 分鐘後狀態「等待批准」，顯示問題「要保留這次的變更嗎？」與「批准 / 退回」。
- `wf.txt` 已存在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H2 批准

步驟：
1. H1 按「批准」。

預期：
- 狀態「完成」，結束節點變綠。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H3 退回（需要 Claude 額度 ×2）

步驟：
1. 再跑一次 H1，這次按「退回」。

預期：
- 狀態「已退回」（中性色，不是紅色失敗），沒有錯誤文字；檔案留著。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H4 取消（需要 Claude 額度）

步驟：
1. 再跑一次，執行中按「取消」→ 確認。

預期：
- 10 秒內狀態「已取消」；正在跑的節點狀態點是中性的「已取消」不是紅色；對應的 CLI 行程被終止（工作階段變已結束）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H5 等待批准時重開 app（需要 Claude 額度 ×2）

步驟：
1. 跑到等待批准，關 app 再開。
2. 按「批准」。

預期：
- 重開後那筆執行還在，仍是等待批准；批准後完成，前面的節點沒有重跑。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H6 執行中關 app（需要 Claude 額度）

步驟：
1. 執行中直接關 app，再開。

預期：
- 那筆執行顯示「失敗」，錯誤「app 在執行途中關閉了」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H7 用量上限欄位

步驟：
1. 打開執行對話框看「用量上限（估算美元）」。
2. 把 Claude 改成 API 金鑰模式（F2）再打開一次。

預期：
- 訂閱模式：留空，佔位「留空不限制」。
- API 金鑰模式：預填 2。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H8 用量上限觸發（需要 Claude 額度）

步驟：
1. 執行範本，用量上限填 `0.01`。

預期：
- 第一個節點跑完後執行變「失敗」，錯誤「超出這次執行的用量上限 (估算 $0.01)」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H9 點節點切終端機

步驟：
1. 任一執行進行中或完成後，在清單點「實作」那一列。

預期：
- 中間切到該節點的終端機，看得到 CLI 的輸出。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H10 執行對話框驗證

步驟：
1. 任務留空按開始；工作目錄留空按開始；工作目錄填不存在的路徑按開始。

預期：
- 三種都留在對話框並顯示原因（請輸入任務內容 / 請輸入工作目錄 / 工作目錄不存在：…）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H11 內建範本清單與說明

步驟：
1. 側欄「工作流」按「執行工作流」，把「工作流」下拉拉開。
2. 由上往下一個一個選過去。

預期：
- 「內建」分組剛好八個，依序是：實作 → 審查 → 批准、軟體開發：需求 → 設計 → 實作 → 測試 → 審查、修 bug：重現 → 修正 → 驗證 → 審查、程式碼審查（唯讀）、補測試、重構：方案 → 批准 → 執行 → 測試、需求分析（只規劃不動碼）、交叉審查：Claude 實作，Codex 審查。
- 下拉底下那一行灰字說明跟著選擇換，例如選「程式碼審查（唯讀）」時是「只看不改：審查指定的範圍或目前工作目錄的變更，依嚴重度列出問題。」。
- 自己存過工作流的話，「自訂」分組排在「內建」後面；沒有說明的自訂工作流那一行是空的。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### H12 跑一次「程式碼審查（唯讀）」（需要 Claude 額度）

步驟：
1. 執行對話框選「程式碼審查（唯讀）」，任務填 `src/main 的錯誤處理`，工作目錄填這個專案的路徑（不要用 8.3 短路徑），用量上限留空 → 開始。

預期：
- 清單出現一筆執行，只有「審查」一個 agent 節點（工作階段名稱「程式碼審查（唯讀） · 審查」）。
- 跑完狀態直接是「完成」，這個範本沒有批准節點，不會停下來問。
- 點「審查」那一列切到它的終端機，看得到依嚴重度列出的問題與檔案行號，最後一行是 `PASS` 或 `FAIL`。
- 工作目錄裡一個檔案都沒有被改到（`git status` 跟跑之前一樣）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## I. 畫布編輯器

### I1 開啟畫布

步驟：
1. 側欄「工作流」旁按「編輯」。

預期：
- 終端機區換成畫布：上方有工作流下拉、名稱、＋Agent ＋條件 ＋批准 ＋結束、刪除 / 儲存 / 儲存並執行；畫布上只有「開始」與「結束」；右側是操作提示。
- 「← 回終端機」切回去，再按「編輯」畫布內容還在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I2 新增與拖曳

步驟：
1. 按 ＋Agent；拖卡片標題到別處。

預期：
- 新卡片出現在最右邊；拖曳流暢，放開時對齊 10 px 格線；卡片被選取有藍框。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I3 接線

步驟：
1. 從「開始」右側圓點按住拖到 Agent 左側圓點放開；再從 Agent 的「成功」拖到「結束」。

預期：
- 出現兩條曲線，端點正好落在圓點上；相鄰很近的節點之間線不會打結。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I4 換線

步驟：
1. 從「開始」的出口再拉一次到「結束」。

預期：
- 原本開始→Agent 那條被換掉，開始只剩一條線指向結束。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I5 不合法的接線

步驟：
1. 把 Agent 的出口拉回 Agent 自己；把某個出口拉到「開始」。

預期：
- 都不接上，畫布上方短暫顯示原因（不能接回自己 / 開始節點不能當終點）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I6 刪節點連帶清理

步驟：
1. 加一個條件節點，來源選那個 Agent；選取 Agent 卡片按 Delete。

預期：
- Agent 與它的線都消失；條件節點的「看誰的輸出」變回未選。
- 選「開始」按 Delete 沒反應。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I7 刪線

步驟：
1. 點一條線（變粗），按 Delete。

預期：
- 只有那條線消失。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I8 Delete 在輸入框

步驟：
1. 選一個 Agent，游標放在右側「提示」欄，按 Delete。

預期：
- 只刪輸入框裡的一個字，節點還在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I9 Agent 屬性

步驟：
1. 選 Agent，右側逐項改：名稱、執行者（四種）、角色、提示、工作目錄、權限（唯讀 / 可修改檔案 / 完全放行）、接續對話、最多幾次、逾時。

預期：
- 改名稱卡片立刻更新；選角色卡片出現角色標籤；執行者變更卡片上的 claude/codex/muse/opencode 字樣；面板最上面顯示節點 id 與 `{{id.text}}` 提示。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I10 角色預設權限

步驟：
1. 角色選「工程師」看「權限」；再選「審查者」。

預期：
- 工程師 → 可修改檔案；審查者 → 唯讀。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I11 條件屬性

步驟：
1. 加條件節點，來源選 Agent，規則選「符合正規式」填 `^PASS$`。

預期：
- 卡片摘要顯示 `agent-1 符合 ^PASS$` 之類；文字過長會以 … 截斷，不會壓到「是 / 否」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I12 批准屬性

步驟：
1. 加批准節點，問題填一段長文字。

預期：
- 卡片顯示問題前段加 …；出口是 批准 / 退回。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I13 驗證錯誤

步驟：
1. 加兩個沒接線的節點、一個提示空白的 Agent、名稱清空，按「儲存」。

預期：
- 畫布上方列出每一條錯誤（走不到、提示不能是空的、工作目錄不能是空的、名稱不能是空的…），不存檔；修正後再存錯誤消失。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I14 儲存與自訂群組

步驟：
1. 做一個合法流程（開始→Agent→結束），名稱 `我的流程`，儲存。
2. 回終端機，按「執行工作流」看下拉。

預期：
- 儲存後「刪除」變可按；執行對話框的下拉多了「自訂」群組含「我的流程」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I15 範本另存副本

步驟：
1. 畫布下拉選「實作 → 審查 → 批准」，改名稱，按「儲存」。
2. 再選一次範本。

預期：
- 7 個節點 7 條線載入；儲存產生一個新的自訂工作流（名稱帶「(副本)」或你改的名稱）；範本本身沒變。
- 載入範本時「刪除」是停用的。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I16 刪除自訂

步驟：
1. 載入 I14 的「我的流程」，按「刪除」→ 確認。

預期：
- 從畫布下拉與執行對話框都消失；`workflows.json` 裡也沒有了。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I17 未儲存確認

步驟：
1. 改動畫布不存，然後在下拉選另一個工作流。

預期：
- 跳出「畫布上的變更還沒儲存，要放棄嗎？」；取消留在原本的，確定才切換。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I18 儲存並執行（需要 Claude 額度）

步驟：
1. 合法流程按「儲存並執行」，填任務與工作目錄 → 開始。

預期：
- 對話框預選剛存的工作流；開始後留在畫布上（見 J 節）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I19 回終端機再回來

步驟：
1. 先開一個 PowerShell，進畫布，再按「← 回終端機」。

預期：
- PowerShell 還活著、畫面滿版可打字；再按「編輯」畫布內容不變。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I20 平移

步驟：
1. 在畫布空白處按住拖曳。

預期：
- 整個畫布跟著移動；放開後節點位置關係不變。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I21 新節點換列

步驟：
1. 連按 ＋Agent 六次。

預期：
- 節點超過畫布右側一定寬度後自動排到下一列，不會一直往右消失。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I22 重啟後自訂還在

步驟：
1. 存一個自訂工作流，關 app 再開，看畫布下拉與執行對話框。

預期：
- 都還在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I23 壞掉的 workflows.json

步驟：
1. 關 app，用編輯器在 `%APPDATA%\myterminal\workflows.json` 的陣列裡加一筆 `{"version":1,"id":"bad","name":"bad","nodes":[{"id":"x"}],"edges":[]}`，再開 app。

預期：
- app 正常啟動；那筆壞的不出現在任何下拉；其他自訂工作流正常。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### I24 新連接時畫布開著

步驟：
1. 畫布開著時按「新連接」開 PowerShell。

預期：
- 建立後自動切回終端機畫面看到新工作階段；再按「編輯」畫布內容還在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## J. 畫布執行檢視

### J1 卡片狀態與用量（需要 網路（OpenCode 免費模型）或 Claude 額度）

步驟：
1. 先做 F10。
2. 畫布做：開始 → Agent（執行者 OpenCode，提示 `Reply with exactly RUNVIEW_OK`，工作目錄 `{{params.cwd}}`）→ 批准 → 結束。
3. 「儲存並執行」，任務隨便填，工作目錄 `D:\tmp\mt-test` → 開始。

預期：
- 留在畫布；Agent 卡片下方出現「執行中」，接著「完成」；編輯列右邊「執行：」顯示狀態徽章與用量；批准卡片變「等待批准」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J2 卡片上的「輸出」

步驟：
1. Agent 完成後按卡片上的「輸出」。

預期：
- 切到該節點的終端機，看得到 `RUNVIEW_OK` 與 `✔ 完成`。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J3 屬性面板接手 / 看輸出

步驟：
1. 按「編輯」回畫布，點 Agent 卡片。

預期：
- 屬性面板最下面有「接手」與「看輸出」，以及「無介面執行中的節點沒辦法追問…」的說明；按接手開互動式 opencode。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J4 卡片上批准

步驟：
1. 批准卡片上按「批准」。

預期：
- 執行變「完成」，結束卡片變「完成」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J5 卡片上退回

步驟：
1. 再跑一次 J1，批准卡片上按「退回」。

預期：
- 執行變「已退回」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J6 執行列取消

步驟：
1. 再跑一次，Agent 執行中時按編輯列的「取消」→ 確認。

預期：
- 執行變「已取消」，卡片狀態中性色。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J7 切來切去狀態保留

步驟：
1. 執行中按「← 回終端機」再按「編輯」。

預期：
- 同一份工作流、卡片上的執行狀態都還在，且會繼續更新。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J8 手動操作：在節點的工作目錄開終端機

步驟：
1. 「編輯」開畫布，加一個 Agent 節點並點起來。
2. 屬性面板「工作目錄」填 `D:	mp\mt-test`（先確定這個目錄存在），「手動操作」的「終端機」留在 PowerShell。
3. 按「開終端機」。

預期：
- 切到終端機畫面，右側多一個「新工作流 · Agent shell」的 PowerShell 工作階段，提示字元就是 `PS D:	mp\mt-test>`。
- 選起來的卡片上那一排（終端／啟動／複製）按下去不會把節點拖走。
- 把「工作目錄」改成 `{{params.cwd}}`（而且這份工作流還沒跑過）再按一次：跳出「工作目錄」對話框，預設填的是上次在執行對話框打的那個；按取消什麼都不會開。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J9 手動操作：開終端機並啟動 CLI（需要 已登入的 Claude）

步驟：
1. 同一個節點，角色選「工程師」，提示填 `請說 SHELL_OK`。
2. 按「開終端機並啟動 Claude」。

預期：
- 開一個「新工作流 · Agent Claude」工作階段，Claude 的 TUI 起在那個工作目錄。
- 「輸入字」面板自動打開，裡面已經填好「工程師的前置指示 + 空行 + 請說 SHELL_OK」，標題寫著送到那個工作階段，但沒有送出；自己按「送出」才會真的交給 Claude。
- 這個節點先前跑過一次工作流的話，CLI 是用 `claude --resume <id>` 起來的（畫面上會接著上一段對話）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### J10 手動操作：複製提示

步驟：
1. 同一個節點按「複製提示」，貼到記事本。

預期：
- 內容是角色前置指示 + 空行 + 提示；提示裡的 `{{params.task}}` 在有執行時被代成那次的任務，沒跑過的話（以及 `{{<節點id>.text}}` 這種上游輸出）原樣留著。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## K. 穩定性與資料

### K1 縮放視窗

步驟：
1. 開兩個工作階段，把視窗縮到最小再最大化。

預期：
- 終端機跟著重排，可繼續打字；不跳「A JavaScript error occurred」。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### K2 連開連關

步驟：
1. 快速連按新連接→建立 6 次開 6 個 PowerShell，再快速全部 ✕。

預期：
- 6 個名稱 1 到 6 不重複；全關後回到空狀態；沒有殘留。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### K3 貼上 200 KB

步驟：
1. 複製約 200 KB 文字貼進 PowerShell。

預期：
- app 本身不凍結（可以按新連接）；終端機可能要花幾秒處理。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### K4 壞掉的 profiles.json

步驟：
1. 關 app，把 `profiles.json` 內容改成 `not json`，開 app。

預期：
- app 正常啟動，已儲存連線為空；存一個新連線後檔案被重寫成正常 JSON。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### K5 唯讀資料夾（需要 會改資料夾權限）

步驟：
1. 把 `%APPDATA%\myterminal` 設為唯讀（拒絕寫入），開 app，儲存一個連線。

預期：
- 顯示儲存失敗的錯誤，不是靜默沒反應。改回權限後恢復。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### K6 執行中關視窗

步驟：
1. 有 Claude 互動工作階段在跑時關視窗。

預期：
- app 幾秒內結束，沒有錯誤框。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：


## L. 打包版特有

### L1 免安裝版的資料位置

步驟：
1. 用免安裝版存一個連線，關掉。
2. 打開 `%APPDATA%\myterminal\profiles.json`。

預期：
- 連線存在那裡（免安裝版與安裝版共用同一個資料夾）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### L2 免安裝版第二次啟動

步驟：
1. 再啟動一次免安裝版。

預期：
- 同樣要等解壓（30 到 60 秒）；之前的設定、主題、自訂工作流都在。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

### L3 CLI 可用性

步驟：
1. 用打包版開 Claude、Codex、Muse、OpenCode 各一個工作階段。

預期：
- 四支都找得到指令（打包版繼承你的使用者 PATH）。

結果：☐ 通過 ☐ 失敗 ☐ 略過　備註：

