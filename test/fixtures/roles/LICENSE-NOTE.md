# 這幾個檔案是從哪裡來的

`engineering/`、`product/`、`testing/` 底下的 markdown 與 `divisions.json`
原封不動抄自 <https://github.com/Mufu/agency-agents>（MIT 授權），
只當成角色庫解析器的測試資料 —— 它們代表使用者真的會指過去的那種資料夾。

換句話說：測試要驗的是「真實世界的檔案長這樣也讀得出來」，
所以請不要為了讓測試好過而修改這些檔案的內容。
